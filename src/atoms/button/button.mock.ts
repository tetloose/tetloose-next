import type { ButtonProps, LinkProps } from './button.types'

export const buttonMock: ButtonProps = {
  label: 'Button',
  variant: 'primary',
  type: 'button',
  disabled: false
}

export const buttonLinkMock: LinkProps = {
  href: '/',
  label: 'Link',
  variant: 'primary'
}
