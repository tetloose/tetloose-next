import { describe, expect, it } from 'vitest'
import { {{camelCase name}} } from './{{kebabCase name}}.utils'

describe('{{camelCase name}}', () => {
  it('returns {{kebabCase name}}', () => {
    expect({{camelCase name}}()).toBe('{{kebabCase name}}')
  })
})
